# Starting multiple actor systems on a single JVM requries some estra tuning
akka.cluster.jmx.multi-mbeans-in-same-jvm = "on"



akka.management.cluster.bootstrap.contact-point-discovery.required-contact-point-nr = 3

akka.discovery.config.services {
  "shopping-cart-service" {
    endpoints = [
      {host = "localhost", port = 19101}
      {host = "localhost", port = 19102}
      {host = "localhost", port = 19103}
    ]
  }
}

